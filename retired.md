# RETIRED — candidates, per amendments-03 §3

Repo Claude, 15 August 2026. **Nothing has been removed from POSTS yet** —
this is the list for the operator to confirm. Numbers are permanent; each
retired entry keeps its PP number and gains the mono note *retired,
superseded by the gizmo catalogue*.

## Recommend retiring (placeholders — nothing behind them)

- **PP-003 BeltMiner (game)** — "Get the game" links to `#`; no game file has
  ever crossed from the factory.
- **PP-007 First animation (video)** — no `src`; body says the piece "docks
  here" but it never docked.
- **PP-015 The Kitchen Sync (text)** — "first instalment incoming"; nothing
  has been serialised.

## Recommend retiring (pre-gizmo demos — fail the screensaver test)

- **PP-004 Belt Ambient 01 (music)** — generative audio deck, no ports,
  cannot be plugged into or from.
- **PP-005 Loader (cracktro)** — ASCII demo, welded canvas + loop, no ports.
- **PP-006 Dustfield (webgl)** — tap-reactive shader, no ports; the definition
  of a screensaver under GIZMO.md ("renders an image and stops... nothing can
  consume it, nothing can drive it").

## Recommend keeping (not gizmos, but not pretenders)

- **PP-001 What this is (text)** — the site's own statement; the site is in
  the catalogue.
- **PP-002 Follow the feed (lock)** — the mailing list; now load-bearing (the
  download gate posts to it).
- **PP-008–PP-00F, PP-016 (images), PP-010–PP-014 (videos)** — finished ice
  from the factories: real published works, not placeholders. Retiring ice
  because it has no ports would also condemn every future edition sold; the
  liquid/ice line says ice is a legitimate catalogue state.
- **PP-017 Tools as art (text)** — the one-paragraph introduction the gizmo
  scroll still needs.

If the operator wants the harder sweep (ice out too, gizmos only), say so and
the same mechanism applies — but that reading would retire 15 finished works,
so it is not assumed here.

## Logged as a separate task (not started)

Quality pass over all 165 gizmos — waits for the reader.js site refactor so
it runs against the final runtime, per amendments-03.
