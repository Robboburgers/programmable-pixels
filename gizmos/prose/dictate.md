# DICTATE — Prose

Source: `brief-prose.md`, Studio Claude, 15 August 2026 — a draft of the
operator's direction, used as the dictation per the brief's own instruction
("build via /gizmo ... with this file as the dictation"). The because,
drafted there and pending the operator's verbatim:

> "it gives no meaning, it just extracts working values you can drive other
> stuff with, from whatever the characters are."

The brief in one line: any text in, signals out. A Transform that walks the
text like a playhead and emits what it is passing over — boundaries, rhythm,
density — never meaning, never pixels.
