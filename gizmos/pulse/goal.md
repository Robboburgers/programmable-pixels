# GOAL — Pulse

Done looks like:

1. **Working** — `pulse.gizmo.json` evaluates in the reader: pull `wave`
   at tick t, get a float in 0..1 that moves over time.
2. **Connectable** — its `wave` out-port wires to a float in-port of
   another gizmo through `reader.js` type matching, and the chain renders:
   PP-0BD.wave -> PP-018.warp (Domain Warp breathes).
3. **Downloadable** — the JSON alone and the folder as a zip, linked from
   the catalogue entry.
4. **Published** — one object appended to POSTS, catalogue number PP-0BD,
   live on the Programmable Pixels site.

Pass condition: all four, verified, pushed to main.
